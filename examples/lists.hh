
# lists a like arrays in other languages with fixed length:
var a;
a = (4) [0, 0, 0, 0];
# if you dont provide enough values in the brackets the remaining one will be filled with 0

# indexing into lists:
println a[3];
# lists are zero-indexed

# reassigning list entries:
a[2] = 3;
println a[2];